import React from 'react';
import Breadcrumb from 'react-bootstrap/Breadcrumb';

const Header = () => {
    return (
        <div>
            <Breadcrumb>
            </Breadcrumb>

        </div>
    );
};

export default Header;